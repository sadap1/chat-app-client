import React from 'react';
import styled from "styled-components";

export default function MessageBox() {
  return <Container>MessageBox</Container>;
}

const Container = styled.div`
    height: 80%;
    z-index: 2;
`